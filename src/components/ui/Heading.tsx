'use client';

import { styled } from '@mui/material';

const Heading = styled('h2')({
  textAlign: 'justify',
  textIndent: '1.5rem',
  fontSize: '1.7rem',
  fontWeight: '700',
  lineHeight: 1.5,
  color: '#000000',
  paddingBottom: '0.2rem',
});

export default Heading;
